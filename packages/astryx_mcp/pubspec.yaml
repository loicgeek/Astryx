name: astryx_mcp
description: A Model Context Protocol (stdio) server exposing the Astryx registry, manifest, templates and swizzle plans to AI agents — sharing astryx_core with the CLI so both surfaces stay in lock-step.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

executables:
  astryx_mcp: astryx_mcp

dependencies:
  astryx_core:

dev_dependencies:
  test: ^1.25.0
