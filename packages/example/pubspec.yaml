name: example
description: Astryx-Flutter gallery — dogfoods tokens, themes, and components.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  astryx_tokens:
  astryx_foundations:
  astryx_themes:
  astryx_widgets:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
