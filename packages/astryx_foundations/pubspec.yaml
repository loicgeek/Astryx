name: astryx_foundations
description: Astryx foundational infra — theme cascade, reduced-motion resolution, breakpoints, and a11y helpers shared by all components.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  astryx_tokens:

dev_dependencies:
  flutter_test:
    sdk: flutter
